{
  "name": "sports-scores",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client"
  ],
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "tsx watch scripts/dev-api.ts",
    "test": "node --import tsx --test \"test/*.test.ts\"",
    "typecheck": "tsc -p ."
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.1122.0",
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/lib-dynamodb": "^3.0.0",
    "@vercel/functions": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.20.4",
    "tsx": "^4.23.15",
    "typescript": "~6.0.3"
  }
}
