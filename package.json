{
  "name": "sports-scores",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client"
  ],
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "node --watch scripts/dev-api.js",
    "test": "node --test \"test/*.test.js\""
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.1122.0",
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/lib-dynamodb": "^3.0.0",
    "@vercel/functions": "^1.0.0"
  }
}
